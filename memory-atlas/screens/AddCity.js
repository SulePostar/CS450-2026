import { View, Text } from 'react-native';

const AddCity = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 36, fontWeight: '700', color: 'blue' }}>Add City</Text>
    </View>
  )
}

export default AddCity;