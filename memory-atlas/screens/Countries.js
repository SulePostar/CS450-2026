import { View, Text } from 'react-native';

const Countries = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 36, fontWeight: '700', color: 'blue' }}>Countries</Text>
    </View>
  )
}

export default Countries;